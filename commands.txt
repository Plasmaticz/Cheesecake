CREATE DATABASE CHEESECAKE;

USE CHEESECAKE;

CREATE TABLE ORDERS (
    ORDERID INT,
    MONTH CHAR(3),
    DAY INT,
    QUANTITY INT,
    TOPPING ENUM('plain', 'chocolate', 'cherry'),
    NOTES VARCHAR(512)
);

DESCRIBE ORDERS;

SELECT COUNT(*) FROM ORDERS;

SELECT COUNT(*) FROM ORDERS WHERE TOPPING = 'plain' AND MONTH = 'Feb';

INSERT INTO ORDERS (ORDERID, MONTH, DAY, QUANTITY, TOPPING, NOTES) VALUES (101, 'Sep', 15, 2, 'cherry', 'Please add extra cherries on top.');
